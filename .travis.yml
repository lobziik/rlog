language: python
python:
  - "3.3"
  - "2.7"
services:
  - redis-server
install:
  - pip install -e .
  - pip install coveralls pytest-cov==1.6

script: py.test --cov . --cov-config .coveragerc

after_success:
  coveralls
